{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the main Grimoire window.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:default"]
}
